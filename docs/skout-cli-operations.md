# Skout CLI and Operations Inventory

## Source Baseline

- Source repository: `<skout-repo>`
- Commit: `cf65984024bd10a0a41faa69b8aecd3894052c31`
- Required state: exact HEAD and clean working tree
- Evidence policy: current executable behavior is required parity; documentation-only contradictions resolve to executable behavior
- Excluded evidence: credentials, tokens, keychain values, databases, generated files, ignored files, and live interactions

## Command Matrix

Field key: “hooks” covers pre-run and post-run behavior; “auto/attr” covers automatic daemon startup and Yahoo attribution; “I/O/exit” covers stdout, stderr, and process status; “live/deferred” separates external checks and later-workstream internals. All commands inherit `-l/--league`, `-d/--debug`, and `-h/--help` unless their execution path ignores those values. No production command declares an alias.

| ID | Capabilities | Visibility and use | Arguments | Local flags | Default and hooks | Auto/attr | I/O/exit | Side effects and errors | Source evidence | Test assertions or explicit gaps | Live/deferred |
|---|---|---|---|---|---|---|---|---|---|---|---|
| CMD-ROOT | CLI-ROOT | Public; `skout` | Subcommand dispatch | `-l/--league`, `-d/--debug`, `-v/--version`, `-h/--help`, `-?` | Custom help; version from `programVersion`; silence usage/errors; hooks auto-start/warn then attribution | Root hook starts daemon except skip set; attribution only for allowlist | Help/version stdout; command errors stderr; error exits 1 | May start daemon before commands; errors print once | `<skout-repo>/cmd/skout/root.go:46-158`, `<skout-repo>/cmd/skout/main.go` | `main_test.go:TestYahooAttributionEligibleCommandCoverage`, `TestYahooAttributionExcludedCommands`, `TestYahooAttributionOmittedAfterCommandError`; gaps: exact help/version/error/exit and `-?` | No live check; command internals deferred by row |
| CMD-LOGIN | CLI-AUTH | Public; `login` | None | None | Yahoo OAuth login; clears backoff after success | Auto-start skipped; no attribution | Provider prompts/status stdout; warning stderr; 0/1 | Browser and keychain through Yahoo boundary; config write; contextual login error | `<skout-repo>/cmd/skout/auth.go:17-29` | `svc_test.go:TestLoginClearsYahooBackoff`; gaps: parsing, streams, exits, browser/keychain contract | Live: OAuth/browser/keychain; provider internals deferred |
| CMD-LOGOUT | CLI-AUTH | Public; `logout` | None | None | Remove Yahoo credentials | Auto-start skipped; no attribution | Provider status stdout; errors stderr; 0/1 | Keychain deletion; contextual logout error | `<skout-repo>/cmd/skout/auth.go:31-40` | Gap: no command assertion | Live: keychain; provider internals deferred |
| CMD-STATUS | CLI-AUTH, OPS-CONFIG | Public; `st` | None | None | Print daemon/database/config/auth state and select league | Auto-start enabled; Yahoo attribution stdout on success | Status/TUI/attribution stdout; warnings/errors stderr; 0/1 | Reads config, keychain boundary, database metadata; may write selected league | `<skout-repo>/cmd/skout/auth.go:42-199` | `svc_test.go:TestPrintYahooBackoffStatus`; gaps: command parsing, full output, exits, TUI selection | Live: keychain/Yahoo/TUI; provider/store details deferred |
| CMD-SYNC | OPS-SYNC | Public; `sync` | None | None | Signal daemon for forced sync and tail new log output | Auto-start enabled; no attribution | Progress/log stdout; contextual errors stderr; 0/1 | SIGUSR1; reads PID/log; provider work occurs in daemon | `<skout-repo>/cmd/skout/sync.go:90-199` | `sync_test.go:TestRunLiveSyncYahooBackoffGateAndForceBypass` and source-specific assertions; gap: thin-client command streams/exits | Live: real daemon/signals; provider/store pipeline deferred |
| CMD-START | OPS-DAEMON | Public; `start` | None | None | Clear disabled marker; preserve live daemon; remove stale PID; fork child | Auto-start skipped; no attribution | Start/already-running stdout; warning stderr; 0/1 | Creates config dir, log and PID; spawns detached child | `<skout-repo>/cmd/skout/svc.go:27-32,182-199,270-299` | Gaps: start parsing, stale PID, fork, streams, exits | Live: subprocess/process group/filesystem |
| CMD-STOP | OPS-DAEMON | Public; `stop` | None | None | Stop daemon; mark disabled; cleanup PID | Auto-start skipped; no attribution | Stop state stdout; stale-PID notice stderr; returns success after signal lands | SIGTERM, timed poll, optional SIGKILL, PID deletion, disabled marker | `<skout-repo>/cmd/skout/svc.go:34-39,200-258` | `reset_test.go:TestResetStopsDaemonAndReleasesDB`; gaps: direct stop branches/streams | Live: signals/process lifecycle |
| CMD-RESTART | OPS-DAEMON | Public; `restart` | None | None | Stop live daemon then start; start directly otherwise | Auto-start skipped; no attribution | Composed stop/start output; errors stderr; 0/1 | Signals and child spawn; disabled marker cleared by start | `<skout-repo>/cmd/skout/svc.go:41-46,260-268` | Gap: no direct restart assertion | Live: signals/subprocess |
| CMD-DAEMON | OPS-DAEMON, OPS-SYNC | Hidden; `_daemon` | None | None | Foreground loop; startup sync if due; 8-hour ticker; forced sync on SIGUSR1 | Auto-start skipped; no attribution | Timestamped log stdout/stderr redirected by parent; 0/1 | Opens store; removes PID on exit; handles signals; rotates log | `<skout-repo>/cmd/skout/svc.go:48-55,329-430` | `svc_test.go:TestAutomaticSyncDueRecentManualRunSuppresses`, `TestAutomaticSyncDueAllowsAfterInterval`, rotation tests; gaps: full loop/exits | Live: signals/time/process; provider/store pipeline deferred |
| CMD-LOG | OPS-DAEMON | Public; `log` | None | `-n/--lines=50`, `-f/--follow`, `-p/--path` | Print path or tail N lines; optional follow; never starts daemon | Auto-start skipped; no attribution | Selected log/path stdout; validation/open errors stderr; 0/1 | Reads log only; follow observes truncation | `<skout-repo>/cmd/skout/log.go:20-112` | `log_test.go:TestShowDaemonLogTailAndMissing`, `TestShowDaemonLogFollowHandlesTruncate`; gaps: flag parsing/path-only/exits | Live: follow against real daemon |
| CMD-RESET | OPS-LOCAL | Public; `reset` | None | None | Confirm unless DB absent; stop daemon before deletion; cancel by default | Auto-start skipped; no attribution | Prompt/result stdout; errors stderr; 0/1 | Stops daemon, deletes database, clears disabled marker for next auto-start | `<skout-repo>/cmd/skout/reset.go:14-55` | `reset_test.go:TestResetStopsDaemonAndReleasesDB`, `TestResetWithoutRunningDaemonDeletesDB`; gaps: cancel/no-DB/exact streams | Live: real daemon excluded; deterministic temp files required |
| CMD-FETCH | CLI-FETCH | Public; `fetch <path>` | Exactly one path | None | Authenticated raw Yahoo GET; pretty-print valid JSON, raw otherwise | Auto-start enabled; Yahoo attribution stderr on success | Payload stdout; attribution/errors stderr; 0/1 | Network request only; no intended local mutation | `<skout-repo>/cmd/skout/fetch.go:12-52` | `main_test.go:TestYahooFetchAttributionPreservesJSONStdout`; gaps: args, JSON/raw/error/exit | Live: Yahoo; provider internals deferred |
| CMD-LM | ADV-LLM | Public; `lm` | None | None | Interactive provider/model/key flow | Auto-start enabled; no attribution | TUI/status stdout; errors stderr; 0/1 | Reads/writes strategy and provider key boundary; may fetch models | `<skout-repo>/cmd/skout/lm.go:17-131` | Gap: no command-level assertion | Live: terminal/keychain/model endpoint; advisory internals deferred |
| CMD-GLOSSARY | CLI-GLOSSARY | Public; `i [term]` | Zero or one term | None | Full glossary, exact match, suggestions, or selector | Auto-start enabled; no attribution | Glossary/TUI stdout; errors stderr; 0/1 | Reads glossary; terminal selection for ambiguity | `<skout-repo>/cmd/skout/whatis.go:13-188` | `glossary_test.go:TestGlossary_SectionsExist`, `TestGlossary_CoverageChecklistComplete`, `TestGlossary_SkoutSignalsPresent`; gaps: parser/streams/exits/TUI | Live: terminal selector; display/advisory glossary internals deferred |
| CMD-ROSTER | CLI-ROSTER | Public; `r [name]` | Zero or one team/manager query | None | Own roster by default; substring match; TUI on ambiguity | Auto-start enabled; Yahoo attribution stdout on success | Tables/TUI/attribution stdout; debug/errors stderr; 0/1 | Reads/writes caches and snapshots through later workstreams | `<skout-repo>/cmd/skout/roster.go:20-342` | `roster_test.go:TestRefreshRostersForDisplay_usesFullLeagueDataForAnySelectedTeam`, `TestRefreshRostersForDisplay_rejectsSelectedTeamOutsideLeague`; gaps: command parser/output/exits | Live: Yahoo/TUI; provider/store/display internals deferred |
| CMD-ROSTER-TOTALS | CLI-TOTALS | Public; `rt` | None | `-w/--weekly[=N\|DATE]`; no-value sentinel `true` | Season totals by default; weekly/date mode when flag present | Auto-start enabled; Yahoo attribution stdout on success | Tables/attribution stdout; errors stderr; 0/1 | Reads provider/store data and snapshots | `<skout-repo>/cmd/skout/roster_totals.go:33-744` | `roster_totals_test.go:TestParseRtDateSpec`, `TestComputeWeekOffset`, weekly/stat tests; gaps: flag parser/output/exits | Live: Yahoo; provider/store/display internals deferred |
| CMD-TEAM | CLI-TEAM | Public; `t [query]` | Zero or one team query | None | All 40-man rosters or one matched team | Auto-start enabled; Yahoo attribution stdout on success | Tables/TUI/attribution stdout; errors stderr; 0/1 | Refreshes stale MLB roster data and reconciles identities | `<skout-repo>/cmd/skout/team.go:22-206` | `team_test.go:TestTeamCmd_help`, `TestTeamCmdHelpReflects40Man`, cache/reconciliation tests; gaps: full output/exits/TUI | Live: MLB/TUI; provider/store/display internals deferred |
| CMD-TEAM-TOTALS | CLI-TOTALS | Public; `tt` | None | None | MLB standings and per-team season totals | Auto-start enabled; Yahoo attribution stdout on success | Tables/attribution stdout; errors stderr; 0/1 | Reads store; may use provider-refreshed data | `<skout-repo>/cmd/skout/teams_totals.go:16-159` | `teams_totals_test.go:TestCountYahooRostered*` assertions; gaps: command parsing/output/exits | Live: MLB/Yahoo; provider/store/display internals deferred |
| CMD-MATCH | CLI-MATCH | Public; `m` | None | `-w/--week N`, `-W/--weekly`, `-D/--day DATE`, `-a/--advise` | Today by default; week implies weekly; advisory opt-in | Auto-start enabled; Yahoo attribution stdout on success | Match tables/attribution stdout; timing debug/errors stderr; 0/1 | Provider/store snapshots and advisory calls through later workstreams | `<skout-repo>/cmd/skout/match.go:67-662` | `match_test.go:TestFlagValidation_WeeklyAndDay`, `TestFlagValidation_WeekImpliesWeekly`, date/timing/circuit/odds tests; gaps: full command output/exits/advisory flag parsing | Live: providers/advisory; internals deferred |
| CMD-SP | CLI-SP | Public; `sp` | None | None | Three-day probable-pitcher slate; current ESPN odds and future OddsShark odds | Auto-start enabled; Yahoo attribution stdout on success | Slate/attribution stdout; degradation/errors stderr as implemented; 0/1 | Reads store; fetches schedule/current and future odds through later workstream | `<skout-repo>/cmd/skout/sp.go:21-205` | `sp_test.go:TestSlateRows*`, `TestFutureOddsFromLines*`, sorting/ownership assertions; gaps: command streams/exits | Live: MLB/ESPN/OddsShark; provider/store/display internals deferred |
| CMD-HITTERS | CLI-PLAYER | Public; `h [N\|name]` | Zero or one nonnegative N/name | `-s/--sort`, `-p/--position`, `-w/--waiver` | Top 20 by Yahoo rank; name detail/TUI; waiver filters | Auto-start enabled; Yahoo attribution stdout on success | Browse/detail/TUI/attribution stdout; debug/errors stderr; 0/1 | May trigger sync; reads/writes store/snapshots | `<skout-repo>/cmd/skout/hitters.go:7-34`, `<skout-repo>/cmd/skout/playerpool.go:128-552` | `hitters_test.go:TestHCmd_help`, `TestHPCanonicals`, `TestRemovedCommands`, `TestHPFlagDefaultsEmpty`; gaps: default count/output/exits | Live: providers/TUI; store/analysis/display internals deferred |
| CMD-PITCHERS | CLI-PLAYER | Public; `p [N\|name]` | Zero or one nonnegative N/name | `-s/--sort`, `-p/--position`, `-w/--waiver` | Top 20 by Yahoo rank; name detail/TUI; waiver filters | Auto-start enabled; Yahoo attribution stdout on success | Browse/detail/TUI/attribution stdout; debug/errors stderr; 0/1 | May trigger sync; reads/writes store/snapshots | `<skout-repo>/cmd/skout/pitchers.go:7-34`, `<skout-repo>/cmd/skout/playerpool.go:128-552` | `pitchers_test.go:TestPCmd_help`, shared `hitters_test.go` canonical/default assertions; gaps: default count/output/exits | Live: providers/TUI; store/analysis/display internals deferred |

## Implemented b9 Surface

The Rust command layer implements every retained public command plus hidden `_daemon` and compatibility alias `whatis`. Shared metadata owns registration and root help; every public command exposes command-specific help. The command-specific comparison rows below remain authoritative for exact parity, tested Rust improvements, live checks, and explicit gaps.

- Preserve `login`, `logout`, `st`, `m`, `r`, `rt`, `t`, `tt`, `sp`, `h`, `p`, `i`, `lm`, `fetch`, `log`, `reset`, `start`, `stop`, and `restart` as public commands.
- Preserve direct foreground `sync`, explicit daemon startup, private socket control, b9-owned state, and embedded glossary data as tested Rust improvements.
- Preserve skout's compact title, uppercase hierarchy, fixed player and MLB columns, semantic 256-color roles, and automatic plain fallback.
- Preserve current, explicit-week, weekly, and ISO-day matchup selection with durable stale and local-only fallbacks.
- Preserve deterministic roster and waiver ordering while leaving PQS, PQT, StartHoldScore, and dependent columns as explicit gaps.
- Keep interactive selector and live-provider behavior pending their manual gates.

## Operational Contracts

| ID | Contract | Observable behavior | Evidence | Assertion or gap | Live boundary |
|---|---|---|---|---|---|
| OP-CONFIG | Configuration | `~/.config/skout/config.json`; absent reads as zero value; parent 0700; temp 0600; indented JSON; atomic same-directory rename; stores league/team and Yahoo circuit state | `<skout-repo>/internal/config/config.go:1-135` | `config_test.go` path, round-trip, permissions, migration assertions | None |
| OP-AUTH | Authentication boundary | `YAHOO_CLIENT_ID`; PKCE login opens browser; redirect pasted to stdin; token keychain lifecycle; successful login clears circuit; status reads token state | `<skout-repo>/internal/yahoo/auth.go`, `<skout-repo>/cmd/skout/auth.go` | Login backoff assertion; gaps: command flow and token boundary | OAuth/browser/keychain/Yahoo |
| OP-FILES | Local state | Config directory contains config, database, PID, disabled marker, and log; debug advisory files use OS temp storage; command-specific permissions follow source | `<skout-repo>/cmd/skout/auth.go:143-199`, `<skout-repo>/cmd/skout/svc.go:67-104`, `<skout-repo>/internal/config/config.go` | Config and daemon unit assertions; gap: consolidated permission matrix | Real user directories excluded |
| OP-DAEMON | Daemon lifecycle | `start` forks detached `_daemon`; automatic start is best effort; stop disables auto-start; restart clears disable through start; stale PID is removed | `<skout-repo>/cmd/skout/svc.go:163-314` | Automatic due and reset integration assertions; gaps: fork/start/restart branches | Real processes/signals |
| OP-SIGNALS | Signal protocol | Parent uses SIGTERM then optional SIGKILL; daemon exits on interrupt/SIGTERM; SIGUSR1 forces a sync; PID liveness uses signal 0 | `<skout-repo>/cmd/skout/svc.go:151-160,200-258,329-390`, `<skout-repo>/cmd/skout/sync.go:106-199` | Reset subprocess assertion; gaps: SIGUSR1 and escalation protocol | Unix process model |
| OP-LOG | Logging | `svc.log`; append child stdout/stderr; timestamps in America/New_York with UTC fallback; truncate in place above 5 MiB; follow detects truncation | `<skout-repo>/cmd/skout/svc.go:22-104,270-299,411-415`, `<skout-repo>/cmd/skout/log.go` | Rotation, tail, missing, and truncate-follow assertions | Real daemon follow |
| OP-RESET | Reset | No DB is success; default confirmation is no; yes stops live daemon, deletes DB, and clears disabled marker; failures carry context | `<skout-repo>/cmd/skout/reset.go` | Running/no-daemon deletion assertions; gaps: no-DB/cancel/exact streams | Real daemon excluded |
| OP-SYNC | Sync routing | `sync` auto-starts daemon, records current log offset, sends SIGUSR1, and tails until completion marker; daemon gates automatic/startup work for eight hours but forced work bypasses TTL | `<skout-repo>/cmd/skout/sync.go:90-199`, `<skout-repo>/cmd/skout/svc.go:329-406` | Backoff/force and automatic-due assertions; gap: thin-client end-to-end | Real daemon/providers |
| OP-TERMINAL | Terminal behavior | Provider/team/name selectors and `lm` require interactive terminal behavior; color and rune-width behavior must preserve observable output where applicable | `<skout-repo>/cmd/skout/tui.go`, `<skout-repo>/cmd/skout/lm.go`, command Long descriptions | Help/glossary assertions only; gap: terminal interaction harness | Real TTY |
| OP-BROWSER | Browser launch | Yahoo login invokes macOS `open` with authorization URL; launch failure is not surfaced before pasted redirect flow | `<skout-repo>/internal/yahoo/auth.go:44-86` | Gap: no browser-launch assertion | macOS browser |
| OP-KEYCHAIN | Keychain | Yahoo token uses service `skout` and user `yahoo-oauth-token`; advisory keys use provider metadata; environment value takes precedence for advisory reads | `<skout-repo>/internal/yahoo/auth.go:19-20,125-203`, `<skout-repo>/internal/advisory/keychain.go` | Gap: boundary adapters not command-tested | OS keychain |
| OP-DEBUG | Debug | Global `-d/--debug` enables raw provider responses and sync/match timing; match timing writes stderr; advisory debug artifacts use OS temp; sensitive payload risk requires redaction review before port | `<skout-repo>/cmd/skout/root.go:106-107`, `<skout-repo>/cmd/skout/match.go:67-75`, provider assignments, `<skout-repo>/internal/advisory/debug_test.go` | Match stderr gate asserted; gaps: provider payload/redaction inventory | Live payloads excluded |
| OP-ATTR | Yahoo attribution | Successful Yahoo-data commands print a blank line and attribution to stdout; `fetch` uses stderr to preserve JSON stdout; errors suppress attribution | `<skout-repo>/cmd/skout/root.go:32-74` | All three attribution behaviors asserted in `<skout-repo>/cmd/skout/main_test.go` | None |

## CLI Source Coverage

| Source | Contracts or disposition |
|---|---|
| `<skout-repo>/README.md` | Documentation claim ledger |
| `<skout-repo>/arch.md` | Documentation claim ledger |
| `<skout-repo>/cmd/skout/auth.go` | CMD-LOGIN, CMD-LOGOUT, CMD-STATUS, OP-AUTH |
| `<skout-repo>/cmd/skout/fetch.go` | CMD-FETCH |
| `<skout-repo>/cmd/skout/glossary_test.go` | CMD-GLOSSARY assertions |
| `<skout-repo>/cmd/skout/hitters.go` | CMD-HITTERS |
| `<skout-repo>/cmd/skout/hitters_test.go` | CMD-HITTERS and CMD-PITCHERS shared assertions |
| `<skout-repo>/cmd/skout/log.go` | CMD-LOG, OP-LOG |
| `<skout-repo>/cmd/skout/log_test.go` | CMD-LOG and OP-LOG assertions |
| `<skout-repo>/cmd/skout/main.go` | CMD-ROOT process entry |
| `<skout-repo>/cmd/skout/main_test.go` | CMD-ROOT and OP-ATTR assertions |
| `<skout-repo>/cmd/skout/match.go` | CMD-MATCH |
| `<skout-repo>/cmd/skout/match_test.go` | CMD-MATCH assertions |
| `<skout-repo>/cmd/skout/mlb_roster_sync.go` | OP-SYNC shell evidence; provider/store internals deferred |
| `<skout-repo>/cmd/skout/mlb_roster_sync_test.go` | OP-SYNC partial-failure assertion; provider/store internals deferred |
| `<skout-repo>/cmd/skout/pitchers.go` | CMD-PITCHERS |
| `<skout-repo>/cmd/skout/pitchers_test.go` | CMD-PITCHERS help assertion |
| `<skout-repo>/cmd/skout/playercard_gamelog.go` | CMD-HITTERS/CMD-PITCHERS shell dependency; analysis/display details deferred |
| `<skout-repo>/cmd/skout/playercard_gamelog_test.go` | Deferred analysis/display assertions |
| `<skout-repo>/cmd/skout/playerpool.go` | CMD-HITTERS, CMD-PITCHERS |
| `<skout-repo>/cmd/skout/reset.go` | CMD-RESET, OP-RESET |
| `<skout-repo>/cmd/skout/reset_test.go` | CMD-RESET, OP-RESET, OP-SIGNALS assertions |
| `<skout-repo>/cmd/skout/root.go` | CMD-ROOT, OP-ATTR, OP-DEBUG, OP-DAEMON |
| `<skout-repo>/cmd/skout/roster.go` | CMD-ROSTER |
| `<skout-repo>/cmd/skout/roster_test.go` | CMD-ROSTER assertions |
| `<skout-repo>/cmd/skout/roster_totals.go` | CMD-ROSTER-TOTALS |
| `<skout-repo>/cmd/skout/roster_totals_test.go` | CMD-ROSTER-TOTALS assertions |
| `<skout-repo>/cmd/skout/snapshot_inventory.go` | OP-SYNC shell evidence; persistence details deferred |
| `<skout-repo>/cmd/skout/snapshot_inventory_test.go` | OP-SYNC dataset assertions; persistence details deferred |
| `<skout-repo>/cmd/skout/sp.go` | CMD-SP |
| `<skout-repo>/cmd/skout/sp_test.go` | CMD-SP odds/slate assertions |
| `<skout-repo>/cmd/skout/svc.go` | CMD-START, CMD-STOP, CMD-RESTART, CMD-DAEMON, OP-DAEMON, OP-SIGNALS, OP-LOG |
| `<skout-repo>/cmd/skout/svc_test.go` | OP-DAEMON, OP-LOG, OP-AUTH assertions |
| `<skout-repo>/cmd/skout/sync.go` | CMD-SYNC, OP-SYNC |
| `<skout-repo>/cmd/skout/sync_fantasypros_test.go` | Provider behavior deferred; mapped as OP-SYNC pipeline evidence only |
| `<skout-repo>/cmd/skout/sync_test.go` | OP-SYNC assertions; provider/store details deferred |
| `<skout-repo>/cmd/skout/team.go` | CMD-TEAM |
| `<skout-repo>/cmd/skout/team_test.go` | CMD-TEAM assertions |
| `<skout-repo>/cmd/skout/teams_totals.go` | CMD-TEAM-TOTALS |
| `<skout-repo>/cmd/skout/teams_totals_test.go` | CMD-TEAM-TOTALS assertions |
| `<skout-repo>/cmd/skout/tui.go` | OP-TERMINAL |
| `<skout-repo>/cmd/skout/whatis.go` | CMD-GLOSSARY |
| `<skout-repo>/docs/glossary.md` | CMD-GLOSSARY reference; terminology contents deferred |
| `<skout-repo>/docs/skout-sync.md` | OP-SYNC reference; proposal-only and historical claims require claim-ledger disposition when matched |
| `<skout-repo>/internal/config/config.go` | OP-CONFIG |
| `<skout-repo>/internal/config/config_test.go` | OP-CONFIG assertions |

## Documentation Claim Extraction

Extraction unit: one source line. Extract every line in `README.md` or `arch.md` matching either a current or historical `skout <command>` token from the known command vocabulary, a backticked long flag matching `--[a-z][a-z-]*`, or a backticked short flag matching `-[A-Za-z?]`. A line containing multiple tokens remains one claim unit so its surrounding assertion is adjudicated together. False positives remain in the ledger with disposition `Extraction false positive`.

No extraction false positives occur at the pinned baseline.

- DC-001 — Source: `<skout-repo>/README.md:11`; Claim: Skout fetches your league, roster, and opponent data from Yahoo, enriches players with live stats from MLB's StatsAPI, Baseball Savant, and FanGraphs, and surfaces matchup-aware guidance — head-to-head category analysis, lineup recommendations, and waiver pickup targets — as plain tabular output in your terminal. The primary workflow centers on `skout m` (match), which assembles the current matchup state and helps you decide what to do today. No writes are made to Yahoo; all output is advisory.; Capabilities: CLI-MATCH; Executable evidence: CMD-MATCH matrix/source evidence; Disposition: Executable parity claim.
- DC-002 — Source: `<skout-repo>/README.md:43`; Claim: skout login; Capabilities: CLI-AUTH; Executable evidence: CMD-LOGIN matrix/source evidence; Disposition: Executable parity claim.
- DC-003 — Source: `<skout-repo>/README.md:46`; Claim: This opens a browser to Yahoo's OAuth consent page. After authorizing, Yahoo redirects to a non-loading localhost page — copy the full URL from your browser's address bar and paste it back into the terminal. The token is stored in your OS keychain. Tokens are valid for ~1 hour and refresh automatically; run `skout login` again if both access and refresh tokens expire.; Capabilities: CLI-AUTH; Executable evidence: CMD-LOGIN matrix/source evidence; Disposition: Executable parity claim.
- DC-004 — Source: `<skout-repo>/README.md:49`; Claim: skout st       # confirm auth, list leagues, and set your active league; Capabilities: CLI-AUTH; Executable evidence: CMD-STATUS matrix/source evidence; Disposition: Executable parity claim.
- DC-005 — Source: `<skout-repo>/README.md:50`; Claim: skout logout   # remove stored token; Capabilities: CLI-AUTH; Executable evidence: CMD-LOGOUT matrix/source evidence; Disposition: Executable parity claim.
- DC-006 — Source: `<skout-repo>/README.md:53`; Claim: `skout st` (status) lets you interactively select which league to work with. The selection is saved to `~/.config/skout/config.json` and used automatically by all commands.; Capabilities: CLI-AUTH; Executable evidence: CMD-STATUS matrix/source evidence; Disposition: Executable parity claim.
- DC-007 — Source: `<skout-repo>/README.md:59`; Claim: skout m                    # head-to-head matchup: today's stats + live game state; Capabilities: CLI-MATCH; Executable evidence: CMD-MATCH matrix/source evidence; Disposition: Executable parity claim.
- DC-008 — Source: `<skout-repo>/README.md:60`; Claim: skout m -W                 # show weekly running totals; Capabilities: CLI-MATCH; Executable evidence: CMD-MATCH matrix/source evidence; Disposition: Executable parity claim.
- DC-009 — Source: `<skout-repo>/README.md:61`; Claim: skout m -D mar-31          # show stats for a specific day; Capabilities: CLI-MATCH; Executable evidence: CMD-MATCH matrix/source evidence; Disposition: Executable parity claim.
- DC-010 — Source: `<skout-repo>/README.md:62`; Claim: skout m -w 3               # matchup for a specific week (implies -W); Capabilities: CLI-MATCH; Executable evidence: CMD-MATCH matrix/source evidence; Disposition: Executable parity claim.
- DC-011 — Source: `<skout-repo>/README.md:65`; Claim: skout r                    # your fantasy roster with stats, YR, 5-cat totals; Capabilities: CLI-ROSTER; Executable evidence: CMD-ROSTER matrix/source evidence; Disposition: Executable parity claim.
- DC-012 — Source: `<skout-repo>/README.md:68`; Claim: skout h                    # top 10 hitters by Yahoo Rank, OWNER column on; Capabilities: CLI-PLAYER; Executable evidence: Settled Drift plus command/source evidence; Disposition: Resolved documentation drift.
- DC-013 — Source: `<skout-repo>/README.md:69`; Claim: skout p                    # top 10 pitchers by Yahoo Rank, OWNER column on; Capabilities: CLI-PLAYER; Executable evidence: Settled Drift plus command/source evidence; Disposition: Resolved documentation drift.
- DC-014 — Source: `<skout-repo>/README.md:70`; Claim: skout h 50                 # top 50 hitters; Capabilities: CLI-PLAYER; Executable evidence: CMD-HITTERS matrix/source evidence; Disposition: Executable parity claim.
- DC-015 — Source: `<skout-repo>/README.md:71`; Claim: skout h <name>             # detail card: identity + SAVANT row + AVG162G/CURRENT splits + GAME LOG (last 10 days); Capabilities: CLI-PLAYER; Executable evidence: CMD-HITTERS matrix/source evidence; Disposition: Executable parity claim.
- DC-016 — Source: `<skout-repo>/README.md:72`; Claim: skout h -w                 # waiver mode: FA-only + MLB-rostered + position-relative PA floor; Capabilities: CLI-PLAYER; Executable evidence: CMD-HITTERS matrix/source evidence; Disposition: Executable parity claim.
- DC-017 — Source: `<skout-repo>/README.md:73`; Claim: skout p -w                 # waiver mode for pitchers (SP / RP role-IP floors); Capabilities: CLI-PLAYER; Executable evidence: CMD-PITCHERS matrix/source evidence; Disposition: Executable parity claim.
- DC-018 — Source: `<skout-repo>/README.md:74`; Claim: skout h -s ops             # sort by any column (yr / ops / name / owner / …); Capabilities: CLI-PLAYER; Executable evidence: CMD-HITTERS matrix/source evidence; Disposition: Executable parity claim.
- DC-019 — Source: `<skout-repo>/README.md:77`; Claim: skout t                    # every MLB team's 40-man roster, hitters / pitchers split; Capabilities: CLI-TEAM; Executable evidence: CMD-TEAM matrix/source evidence; Disposition: Executable parity claim.
- DC-020 — Source: `<skout-repo>/README.md:78`; Claim: skout t pirates            # filter by abbr / city / nickname; Capabilities: CLI-TEAM; Executable evidence: CMD-TEAM matrix/source evidence; Disposition: Executable parity claim.
- DC-021 — Source: `<skout-repo>/README.md:79`; Claim: skout tt                   # MLB standings + per-team season totals; Capabilities: CLI-TOTALS; Executable evidence: CMD-TEAM-TOTALS matrix/source evidence; Disposition: Executable parity claim.
- DC-022 — Source: `<skout-repo>/README.md:82`; Claim: skout sync                 # full live refresh (rosters, FAs, stats, schedule, transactions, injuries, rolling 5-year history); Capabilities: OPS-SYNC; Executable evidence: CMD-SYNC matrix/source evidence; Disposition: Executable parity claim.
- DC-023 — Source: `<skout-repo>/README.md:85`; Claim: skout start                # start background sync daemon; Capabilities: OPS-DAEMON; Executable evidence: CMD-START matrix/source evidence; Disposition: Executable parity claim.
- DC-024 — Source: `<skout-repo>/README.md:86`; Claim: skout stop                 # stop background sync daemon; Capabilities: OPS-DAEMON; Executable evidence: CMD-STOP matrix/source evidence; Disposition: Executable parity claim.
- DC-025 — Source: `<skout-repo>/README.md:87`; Claim: skout restart              # restart background sync daemon; Capabilities: OPS-DAEMON; Executable evidence: CMD-RESTART matrix/source evidence; Disposition: Executable parity claim.
- DC-026 — Source: `<skout-repo>/README.md:88`; Claim: skout log                  # show log path + last 50 daemon log lines; Capabilities: OPS-DAEMON; Executable evidence: CMD-LOG matrix/source evidence; Disposition: Executable parity claim.
- DC-027 — Source: `<skout-repo>/README.md:89`; Claim: skout log -n 100           # show a different number of lines; Capabilities: OPS-DAEMON; Executable evidence: CMD-LOG matrix/source evidence; Disposition: Executable parity claim.
- DC-028 — Source: `<skout-repo>/README.md:90`; Claim: skout log -f               # follow the daemon log without starting the daemon; Capabilities: OPS-DAEMON; Executable evidence: CMD-LOG matrix/source evidence; Disposition: Executable parity claim.
- DC-029 — Source: `<skout-repo>/README.md:93`; Claim: skout fetch "/league/469.l.170874/players;sort=AR;count=3"  # raw Yahoo API GET; Capabilities: CLI-FETCH; Executable evidence: CMD-FETCH matrix/source evidence; Disposition: Executable parity claim.
- DC-030 — Source: `<skout-repo>/README.md:96`; Claim: **Command roles:** `skout m` (match) is the decision surface — it shows your matchup and (in future releases) a prioritized action summary. `skout r` (roster) inspects your team. `skout h` / `skout p` browse the hitter and pitcher pools respectively (default: all players incl. owned, OWNER column shown). Add `-w` to either for waiver mode — FA-only + MLB-rostered + position-relative PA floor (hitters; catchers gated vs. catchers) / role-IP floor (pitchers).; Capabilities: CLI-MATCH, CLI-ROSTER, CLI-PLAYER; Executable evidence: CMD-MATCH matrix/source evidence; Disposition: Executable parity claim.
- DC-031 — Source: `<skout-repo>/README.md:98`; Claim: When Yahoo is unavailable, `skout m` uses complete durable Yahoo snapshots when present and labels their timestamp. Without a usable scoreboard snapshot it shows a local-roster view from stored and non-Yahoo data, omitting opponent totals, matchup analysis, and LLM advice.; Capabilities: CLI-MATCH; Executable evidence: CMD-MATCH matrix/source evidence; Disposition: Executable parity claim.
- DC-032 — Source: `<skout-repo>/README.md:101`; Claim: Use `--debug` / `-d` on any command to print raw API responses to stderr.; Capabilities: CLI-ROOT; Executable evidence: CMD-ROOT matrix/source evidence; Disposition: Executable parity claim.
- DC-033 — Source: `<skout-repo>/README.md:108`; Claim: skout m; Capabilities: CLI-MATCH; Executable evidence: CMD-MATCH matrix/source evidence; Disposition: Executable parity claim.
- DC-034 — Source: `<skout-repo>/README.md:116`; Claim: skout h -w; Capabilities: CLI-PLAYER; Executable evidence: CMD-HITTERS matrix/source evidence; Disposition: Executable parity claim.
- DC-035 — Source: `<skout-repo>/README.md:122`; Claim: skout h <name>; Capabilities: CLI-PLAYER; Executable evidence: CMD-HITTERS matrix/source evidence; Disposition: Executable parity claim.
- DC-036 — Source: `<skout-repo>/arch.md:5`; Claim: Skout is a read-only decision-support CLI for Yahoo Fantasy Baseball. It fetches league, roster, and opponent data from Yahoo's Fantasy Sports API (OAuth2), enriches players with standard statistics from the MLBAM StatsAPI and advanced Statcast metrics from Baseball Savant, and assembles matchup-aware context — head-to-head category analysis, lineup and roster signals, and waiver targets — as plain tabular output. The primary command is `skout m` (match), which displays the current matchup state. No writes are made to Yahoo; all output is advisory.; Capabilities: CLI-MATCH; Executable evidence: CMD-MATCH matrix/source evidence; Disposition: Executable parity claim.
- DC-037 — Source: `<skout-repo>/arch.md:17`; Claim: - Default to **best-effort advice**, not interactive questioning, during normal `skout m` (match) runs; Capabilities: CLI-MATCH; Executable evidence: CMD-MATCH matrix/source evidence; Disposition: Executable parity claim.
- DC-038 — Source: `<skout-repo>/arch.md:103`; Claim: Yahoo's Fantasy Sports API is protected by OAuth2 (3-legged PKCE flow). `skout login` opens the browser to Yahoo's auth page, then prompts the user to paste the redirect URL. The authorization code is extracted and exchanged for an access token, stored in the OS keychain via `github.com/zalando/go-keyring`. Tokens expire after ~1 hour; `savingTokenSource` auto-refreshes using the stored refresh token. Required env var: `YAHOO_CLIENT_ID` (public-client PKCE — no secret needed).; Capabilities: CLI-AUTH; Executable evidence: CMD-LOGIN matrix/source evidence; Disposition: Executable parity claim.
- DC-039 — Source: `<skout-repo>/arch.md:105`; Claim: Commands: `skout login`, `skout logout`, `skout st` (status), `skout m` (match — `-w` for week), `skout h` (hitters) / `skout p` (pitchers; each with `-w` for waiver mode). Auth commands are top-level (no `auth` parent). `skout st` also displays leagues and allows interactive selection via arrow-key TUI. A terminal Yahoo 401/403 or five failed requests opens a persisted circuit; successful requests do not consume the five-failure cycle budget. An open circuit blocks automatic/manual Yahoo sync work, `skout m` live calls, and `skout st` league discovery until a successful `skout login` clears it.; Capabilities: CLI-AUTH, CLI-MATCH, CLI-PLAYER; Executable evidence: CMD-LOGIN matrix/source evidence; Disposition: Executable parity claim.
- DC-040 — Source: `<skout-repo>/arch.md:107`; Claim: **`skout m`** (match) is the flagship command — the primary decision surface. Default mode shows today's per-player stats (MLBAM `byDateRange` + live boxscores); `-W` shows weekly running totals; `-D <date>` shows a specific day within the matchup week. Team summary scoreboard always shows weekly totals (the matchup score). Complete durable Yahoo snapshots preserve the normal matchup during an outage and render a snapshot-time warning. Without a usable scoreboard snapshot, a local-only roster view uses stored roster plus non-Yahoo data and omits opponent totals, category analysis, path-to-win output, and LLM advice. Weekly totals are fed to `MatchContext` only in the full matchup path. `--advise` / `-a` enables the LLM summary. **`skout lm`** configures the LLM provider via arrow-key TUI and stores API keys in the keychain. **`skout r`** (roster) is a fantasy-team inspection surface (stats, PQI, PQT, YR, CR). **`skout h`** (hitters) and **`skout p`** (pitchers) browse the per-type player pool — default top 10 by Yahoo Rank, all players incl. owned, OWNER column on; `-s <col>` sorts by any displayed column (incl. `name` / `owner` alphabetical); `-p <pos>` filters eligible position; a non-integer positional routes to the player detail card (type-agnostic name search). `-w` toggles waiver mode: FA-only + MLB-rostered + role-PA / IP floor + drops NA / SUSP / IL\*. **`skout t`** (MLB team) lists each team's 40-man; IL'd players (D-prefix MLB status) render in dark yellow matching `skout m`, off-active roster members (MIN, RM, ...) render in dark gray, and active 26-man players render in full color.; Capabilities: CLI-MATCH, ADV-LLM, CLI-ROSTER, CLI-PLAYER, CLI-TEAM; Executable evidence: Settled Drift plus command/source evidence; Disposition: Resolved documentation drift.
- DC-041 — Source: `<skout-repo>/arch.md:113`; Claim: TTL groups: `league_settings` (24h), `teams` (1h), `rosters`/`free_agents`/`transactions` (15min), `mlbam_players` (24h), `mlbam_hitting`/`mlbam_pitching` (1h), `mlbam_qs` (24h), `mlb_team_rosters` (24h — populates `mlb_team_active_rosters` for `skout t` (MLB team) and `skout h -w` / `skout p -w` (waiver)'s MLB-rostered filter), `mlb_odds` (30min — sportsbook lines for `skout odds` and `skout m` (match) SP cells, lazy-refreshed via The Odds API), `savant_enrichment` (24h), `fangraphs_stats` (24h), `fangraphs_closers` (12h — RosterResource closer-depth chart override for `MarkClosers`), `fantasypros_ecr` (12h), `schedule` (5min peak / 1h off-peak), `compute_pqs` (1h).; Capabilities: CLI-TEAM, CLI-PLAYER, PRODUCT, CLI-MATCH; Executable evidence: Settled Drift plus command/source evidence; Disposition: Resolved documentation drift.
- DC-042 — Source: `<skout-repo>/arch.md:154`; Claim: The decision-context layer assembles structured context for the advisory summary. It sits between the signal layer and display (and the LLM advisory layer), transforming signals into decision-relevant objects. `BuildMatchContext` produces a JSON-serializable `MatchContext` containing: category gaps and flippability (`ComputeCategoryGaps`), lineup swap candidates with position eligibility and pitcher probable-starter checks (`ComputeLineupCandidates`), uncovered slot gaps (`ComputeSlotGaps`), roster move candidates ranked by matchup category fit from the full FA pool (`ComputeRosterMoves`), per-player StartHoldScore (`ComputeAllSHS` — 0-100, combining PQS 25%, PQT 20%, CatFit 30%, SchedVol 15%, RoleSec 10%), league-level context, risk alerts, and user strategy config loaded from `~/.config/skout/strategy.yaml`. Rendered as the MATCHUP ANALYSIS section at the bottom of `skout m` (match); SHS also displayed as a column in `skout r` (roster).; Capabilities: CLI-MATCH, CLI-ROSTER; Executable evidence: CMD-MATCH matrix/source evidence; Disposition: Executable parity claim.
- DC-043 — Source: `<skout-repo>/arch.md:162`; Claim: Five LLM providers behind a vendor-neutral `Advisor` interface: Gemini, Groq/Llama, Mistral (free tier), Claude, OpenAI (paid). `skout lm` TUI selects provider, stores API keys in macOS keychain, and for OpenAI fetches the live model list from `/v1/models` for interactive model selection (see [`docs/api-openai.md`](docs/api-openai.md)). Provider-specific divergence: OpenAI uses `max_completion_tokens` (required by GPT-5+ models); Groq/Mistral use `max_tokens`. The LLM receives structured action sources (lineup_candidates, roster_moves) as the only valid action pools — it cannot invent actions outside them. Response is structured JSON: confirmations (current-state truths), urgent/overnight (SummaryAction objects with grounding), category_strategy (echoed from payload), risks. Partial-parse failure policy: each top-level field parsed independently, invalid action items discarded individually. `--debug` / `-d` writes `advisory-context.json` and `advisory-response.json` to `os.TempDir()/skout-debug/` (payload only, no API keys). `--no-advise` / `-A` skips the LLM call.; Capabilities: ADV-LLM, CLI-MATCH, CLI-ROOT; Executable evidence: Settled Drift plus command/source evidence; Disposition: Resolved documentation drift.
- DC-044 — Source: `<skout-repo>/arch.md:166`; Claim: `internal/display/table.go` handles roster, waiver wire, and player-list CLI rendering. All player displays use a unified "Name TEAM" merged column. Roster table (HITTER/PITCHER header sections) with columns: SLOT, PLAYER, POS, B/T, STATUS, YR, CR, PQI, SHS, PP, PQT, %OWN, UR, stats. POS is a 5-char cell rendered by `compressPositions` (`internal/display/poscell.go`): literal eligibility list when it fits (≤ 5 visible chars including commas), else single-letter compression in defensive-value order — see the `pos` glossary entry. Waiver wire (WAIVER WIRE header, no SLOT column) shows 12 recommendations per group. Player browse (`skout h` / `skout p`) renders one homogeneous section (hitters or pitchers); a non-integer arg routes to the detail card.; Capabilities: CLI-PLAYER; Executable evidence: CMD-HITTERS matrix/source evidence; Disposition: Executable parity claim.
- DC-045 — Source: `<skout-repo>/arch.md:170`; Claim: `internal/display/matchup.go` handles matchup rendering (`skout m`). Side-by-side team comparison: team summary (weekly category totals with yellow winner highlights, centered SCORE column), team dividers (name/record/rank/remaining games centered with hyphens), HITTER and PITCHER tables split into active/BN/IL groups with independent alignment. First-initial player names ("O Cruz PIT") for space efficiency; H/AB column (gray) for hitters, IP column (gray) for pitchers. NOTES column shows: lineup indicators (green `S` for starting pitcher, green batting order number for hitters in lineup, red `X` for not starting), live game state (inning abbreviation, team-perspective score, batting order position), scheduled game time (v/@ opponent), or "Final". Pre-game players show all dashes for stats. BN/IL rows rendered in gray via `grayWrap` (preserves inner highlight colors); bench batting order in dark green (`GrnD`). POS column shared between sides.; Capabilities: CLI-MATCH; Executable evidence: CMD-MATCH matrix/source evidence; Disposition: Executable parity claim.
- DC-046 — Source: `<skout-repo>/arch.md:198`; Claim: \| `mlb_team_active_rosters` \| MLBAM \| `(team_abbr, mlbam_id, primary_type)` \| Per-team 40-man roster cache (active + IL + minors; each row carries a `status` code where `A` = active, `D60`/`D10`/`D15` = IL, `MIN` = minors, plus a `jersey_number` for the reconciler's `jersey+team` tier) backing `skout t` (MLB team) and `skout tt` (totals); lazy-refreshed every 24h via `FetchActiveRoster()` (now `rosterType=40Man`); two-way players carry one row per `primary_type` ('H' / 'P'). Waiver gating (`MLBRosteredIDsByRole`, `MLBRosteredStatLookups`) filters to `status = 'A'`. Each refresh tail-reconciles against `players`: missing MLBAM IDs are resolved via `/people?personIds=…` and inserted with `mlbam_match_source = '40man'`; present-but-stale `mlb_team` values are updated to match the 40-man parent team. \|; Capabilities: CLI-TEAM, CLI-TOTALS; Executable evidence: CMD-TEAM matrix/source evidence; Disposition: Executable parity claim.
- DC-047 — Source: `<skout-repo>/arch.md:199`; Claim: \| `mlb_odds` \| The Odds API \| `(game_pk, market, side, player_mlbam_id, sportsbook)` \| Per-game sportsbook lines (moneyline, total, pitcher_strikeouts) backing `skout odds` and the `skout m` (match) SP context cells; lazy-refreshed every 30 minutes via `oddsapi.FetchGameLines` + per-event `FetchPitcherKProps`; v1 sources DraftKings only; `player_mlbam_id` defaults to 0 (sentinel) for non-pitcher-prop markets \|; Capabilities: PRODUCT, CLI-MATCH; Executable evidence: Settled Drift plus command/source evidence; Disposition: Resolved documentation drift.
- DC-048 — Source: `<skout-repo>/arch.md:354`; Claim: \| `skout sync` \| Thin client → signals daemon via SIGUSR1 \| Tails svc.log in real-time (`triggerSyncAndTail`) \| Written by daemon's `svcLog` \|; Capabilities: OPS-SYNC; Executable evidence: CMD-SYNC matrix/source evidence; Disposition: Executable parity claim.
- DC-049 — Source: `<skout-repo>/arch.md:355`; Claim: \| `skout log` \| Read-only CLI; never auto-starts daemon \| Absolute path + last 50 lines by default; `-f` follows \| Read only \|; Capabilities: OPS-DAEMON; Executable evidence: CMD-LOG matrix/source evidence; Disposition: Executable parity claim.
- DC-050 — Source: `<skout-repo>/arch.md:361`; Claim: `skout log -f` detects this truncate-in-place rotation, seeks to the new beginning, and continues following appended complete lines.; Capabilities: OPS-DAEMON; Executable evidence: CMD-LOG matrix/source evidence; Disposition: Executable parity claim.
- DC-051 — Source: `<skout-repo>/arch.md:363`; Claim: **Imperative stop (AC114):** `skout stop` sends SIGTERM, polls for 3 s, escalates to SIGKILL if the process is still alive, then unconditionally removes the PID file and marks the service disabled. Always returns `nil` once a signal lands. Worst case is mid-sync interruption, which the next sync re-fetches — every MLBAM/Yahoo sync is idempotent.; Capabilities: OPS-DAEMON; Executable evidence: CMD-STOP matrix/source evidence; Disposition: Executable parity claim.
- DC-052 — Source: `<skout-repo>/arch.md:385`; Claim: \|   +-- roster.go        # skout r (roster) -- DB-backed roster display (no pickups); Capabilities: CLI-ROSTER; Executable evidence: CMD-ROSTER matrix/source evidence; Disposition: Executable parity claim.
- DC-053 — Source: `<skout-repo>/arch.md:386`; Claim: \|   +-- match.go         # skout m (match) [-w N] [-A] -- head-to-head matchup + advisory; Capabilities: CLI-MATCH; Executable evidence: Settled Drift plus command/source evidence; Disposition: Resolved documentation drift.
- DC-054 — Source: `<skout-repo>/arch.md:387`; Claim: \|   +-- lm.go            # skout lm -- LLM provider selection TUI; Capabilities: ADV-LLM; Executable evidence: CMD-LM matrix/source evidence; Disposition: Executable parity claim.
- DC-055 — Source: `<skout-repo>/arch.md:388`; Claim: \|   +-- hitters.go       # skout h -- hitter pool browse (+ detail card via <name>); Capabilities: CLI-PLAYER; Executable evidence: CMD-HITTERS matrix/source evidence; Disposition: Executable parity claim.
- DC-056 — Source: `<skout-repo>/arch.md:389`; Claim: \|   +-- pitchers.go      # skout p -- pitcher pool browse (+ detail card via <name>); Capabilities: CLI-PLAYER; Executable evidence: CMD-PITCHERS matrix/source evidence; Disposition: Executable parity claim.
- DC-057 — Source: `<skout-repo>/arch.md:391`; Claim: \|   +-- sync.go          # skout sync -- mode-aware pipeline (live/events/history); Capabilities: OPS-SYNC; Executable evidence: CMD-SYNC matrix/source evidence; Disposition: Executable parity claim.
- DC-058 — Source: `<skout-repo>/arch.md:392`; Claim: \|   +-- team.go          # skout t (MLB team) -- per-team active-roster view; Capabilities: CLI-TEAM; Executable evidence: CMD-TEAM matrix/source evidence; Disposition: Executable parity claim.
- DC-059 — Source: `<skout-repo>/arch.md:393`; Claim: \|   +-- fetch.go         # skout fetch <path> -- raw Yahoo API debug command; Capabilities: CLI-FETCH; Executable evidence: CMD-FETCH matrix/source evidence; Disposition: Executable parity claim.
- DC-060 — Source: `<skout-repo>/arch.md:394`; Claim: \|   +-- reset.go         # skout reset -- deletes skout.db with confirmation; Capabilities: OPS-LOCAL; Executable evidence: CMD-RESET matrix/source evidence; Disposition: Executable parity claim.
- DC-061 — Source: `<skout-repo>/arch.md:439`; Claim: skout r --league mlb.l.12345; Capabilities: CLI-ROSTER, CLI-ROOT; Executable evidence: CMD-ROSTER matrix/source evidence; Disposition: Executable parity claim.
- DC-062 — Source: `<skout-repo>/arch.md:461`; Claim: skout h [N\|name] / skout p [N\|name]; Capabilities: CLI-PLAYER; Executable evidence: CMD-HITTERS matrix/source evidence; Disposition: Executable parity claim.
- DC-063 — Source: `<skout-repo>/arch.md:475`; Claim: skout m [-w N]; Capabilities: CLI-MATCH; Executable evidence: CMD-MATCH matrix/source evidence; Disposition: Executable parity claim.
- DC-064 — Source: `<skout-repo>/arch.md:506`; Claim: skout sync  (thin client); Capabilities: OPS-SYNC; Executable evidence: CMD-SYNC matrix/source evidence; Disposition: Executable parity claim.

## Settled Drift

| ID | Current parity | Historical rationale | Catalog action |
|---|---|---|---|
| CF-001 | Do not restore standalone `skout odds`; preserve current moneyline context in `m` and current slate-oriented `sp` behavior using ESPN for current games and OddsShark for future games | The v0.52.0 command used a key- and quota-constrained provider; totals and strikeout props disappeared when the provider was replaced; odds context was narrowed and relocated | Resolve as documented-only retired surface; keep reachable capabilities Required |
| CF-002 | Preserve executable default of 20 results for `h` and `p` | README and architecture still say 10 | Resolve documentation drift |
| CF-003 | Preserve executable opt-in `--advise/-a` | One architecture paragraph still says opt-out `--no-advise/-A` | Resolve documentation drift |

All executable capability dispositions remain `Required`. A Director decision is required only for an intentional deviation from current executable behavior or a conflict between executable paths.

## Design Recommendations

| ID | Recommendation | Evidence | Compatibility impact | Tradeoffs | Affected capabilities | Decision status |
|---|---|---|---|---|---|---|
| DR-001 | Generate parser registration and help presentation from one command metadata model | `<skout-repo>/cmd/skout/root.go`, CF-002, CF-003 | Preserve current parsing/help text except settled stale claims | Reduces drift; constrains custom formatting behind one model | CLI-ROOT and all command shells | Shared descriptors, Skout-style root help, and command-specific help are implemented for every retained command |
| DR-002 | Put browser, keychain, signal, PID, process, and terminal mechanics behind explicit platform boundaries | `<skout-repo>/internal/yahoo/auth.go`, `<skout-repo>/cmd/skout/svc.go`, `<skout-repo>/cmd/skout/tui.go`, `<skout-repo>/internal/advisory/keychain.go` | Preserve current macOS behavior exactly | Adds interfaces/test doubles; enables deterministic tests and later portability | CLI-AUTH, OPS-DAEMON, OPS-CONFIG, ADV-LLM | Recommended for later implementation design; no platform expansion authorized |
| DR-003 | Separate daemon control, sync triggering, progress following, and sync execution into bounded orchestration seams | `<skout-repo>/cmd/skout/svc.go`, `<skout-repo>/cmd/skout/sync.go` | Preserve commands, timing gates, signals, output, and failure behavior | More boundaries and fixtures; reduces coupling and makes protocol tests possible | OPS-DAEMON, OPS-SYNC | Recommended for a later implementation AC; not authorized here |

No crate, framework, or compatibility change is selected.

## Current b9 Roster Surface

- `b9 r [name]` renders the configured roster or one deterministic team or manager-name match.
- `b9 rt -w/--weekly[=N|DATE]` selects the requested totals period while preserving season totals as the default.
- `b9 h [N|name]` and `b9 p [N|name]` browse the durable player pool or render a type-agnostic player card.
- Player cards show Yahoo identity and ownership, durable season statistics, and a recent MLB game log.
- Free-agent state is fetched as complete pagination and replaces the prior player-pool snapshot atomically.
- Player-card game logs refresh from MLB when available and render a labeled durable fallback after a refresh failure.
- Waiver mode admits only free agents on the active 26-man roster that clear the predecessor-compatible 60th-percentile hitter, starter, or reliever usage floor.

## Current b9 Operations Surface

- `b9 fetch <path>` performs one authenticated, bounded Yahoo GET, pretty-prints JSON, preserves other response bytes, and keeps attribution on stderr.
- `b9 reset` confirms before deleting only b9's database and stale private runtime control state; it preserves configuration, cache, log, credentials, and every skout file.
- `b9 log` provides bounded tail, follow, and path modes over an owner-only log capped at 5 MiB.
- `b9 start`, `b9 stop`, and `b9 restart` explicitly manage one detached synchronization daemon through held owner locks and an owner-only Unix control socket.
- No unrelated command starts the daemon. `b9 sync -f/--force` remains a direct foreground operation and shares its synchronization service with startup and scheduled daemon work.
- `_daemon` remains hidden; process identity files are diagnostic only and never authorize signaling or shutdown.
- `b9 lm` configures None, Gemini, Groq/Llama, Mistral, Claude, or OpenAI interactively, keeps secrets outside configuration, and uses bounded provider validation and OpenAI model discovery.

## Verification Strategy

### Deterministic pre-release tests

- Generate command parsing and help snapshots from the command contract rows.
- Assert exact version, attribution, stdout, stderr, and exit behavior.
- Use temporary home/config/database/log paths for configuration, permissions, reset, PID, disabled-marker, and log tests.
- Use process and signal test doubles for daemon state transitions, escalation, forced sync, and cleanup.
- Use injected clocks for automatic-sync gates, timeouts, log timestamps, and rotation.
- Use terminal and browser adapters for non-live command routing.
- Convert every explicit gap in the command and operational tables into a named test or a justified later-workstream test.

### Live checks

- Exercise Yahoo OAuth, browser launch, and OS keychain only with Director authorization and non-production credentials.
- Exercise Yahoo-backed status and fetch only after provider/storage deterministic coverage exists.
- Exercise real daemon detach, signals, PID lifecycle, and log follow in an isolated temporary profile.
- Exercise terminal selectors in a supported interactive terminal.
- Keep live failures from substituting for deterministic pre-release regression coverage.

### b9 status boundary

- Keep `b9 st` local-first: it reads cached configuration and durable status only.
- Require explicit `b9 login` or `b9 sync` before accessing Yahoo or the macOS Keychain.
- Render an empty store as unavailable freshness and identity state with `No local snapshot; run b9 sync.` rather than fabricated zero totals.
- Preserve the last successful snapshot and report bounded provider failures locally.
- Render the dashboard fields in settled, fixed order: service state and uptime, last/next scheduled run and completion state, database path/size/schema, MLB/Yahoo identity counts, provider freshness, circuit state and bounded last error, unmatched-player count, then selected league and config paths.
- Persist the daemon's next scheduled synchronization time to the durable store at startup and after every reschedule so `b9 st` can report it without contacting the running daemon process.
- Read an unmigrated, older-schema database without erroring: report its actual schema version and fall back to default dashboard fields rather than requiring a migration to read status.

## Proposed Rust Implementation Slices

| Slice | Prerequisites | Delivered contracts | Required tests | Explicit exclusions |
|---|---|---|---|---|
| CLI-1 Command model | Ratified inventory | CMD-ROOT metadata, parsing, help, version, error routing, OP-ATTR | Parser/help/version/stream/exit snapshots | Complete deterministic shell: shared descriptors, global flags, command help, compatibility alias, hidden daemon, streams, and classified exits implemented; live provider outcomes remain separately gated |
| CLI-2 Local operations | CLI-1; providers/storage inventory for shared persistence boundaries | OP-CONFIG, CMD-LOG path/tail core, CMD-RESET filesystem core | Temp-profile permissions, atomic config, log, reset/cancel tests | Real keychain/browser/signals/providers |
| CLI-3 Platform boundaries | CLI-1; explicit implementation design approval | Browser/keychain/terminal/process/signal interfaces preserving macOS behavior | Adapter contract tests and macOS-focused deterministic doubles | New platform support claims |
| CLI-4 Daemon protocol | CLI-2 and CLI-3; providers/storage sync contract | CMD-START/STOP/RESTART/DAEMON/SYNC and OP-DAEMON/OP-SIGNALS/OP-LOG/OP-SYNC | State-machine, PID, signal, timing, rotation, progress, error tests | Provider pipeline implementation |
| CLI-5 Command shells | CLI-1; applicable provider/storage and analysis/display/advisory inventories Ratified | Remaining public command parsing, routing, flags, hooks, and exact shell I/O | One contract suite per command plus every recorded gap | Deferred internal algorithms and external calls |

CLI and operations implementation remains blocked until this inventory is Ratified. Each slice still requires its own governed implementation AC.
